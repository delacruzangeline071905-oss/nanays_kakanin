# Notification System - Flow Diagrams

## 📊 System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    Notification System                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐         ┌──────────────┐                 │
│  │   Signals    │────────▶│ Notification │                 │
│  │  (Auto)      │         │    Model     │                 │
│  └──────────────┘         └──────┬───────┘                 │
│                                   │                          │
│  ┌──────────────┐                │                          │
│  │   Utility    │────────────────┘                          │
│  │  Functions   │                                           │
│  │  (Manual)    │                                           │
│  └──────────────┘                                           │
│                                                              │
│                   ┌──────────────┐                          │
│                   │   Context    │                          │
│                   │  Processor   │                          │
│                   └──────┬───────┘                          │
│                          │                                   │
│              ┌───────────┴───────────┐                      │
│              ▼                       ▼                       │
│      ┌──────────────┐        ┌──────────────┐              │
│      │ Admin Navbar │        │ User Navbar  │              │
│      │ (user=None)  │        │(user=User)   │              │
│      └──────────────┘        └──────────────┘              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow 1: User Places Order

```
┌──────────┐
│   USER   │
└────┬─────┘
     │ 1. Submits order form
     ▼
┌─────────────────┐
│  checkout_cart  │
│     (view)      │
└────┬────────────┘
     │ 2. Creates Order
     ▼
┌─────────────────┐
│ Order.objects   │
│   .create()     │
└────┬────────────┘
     │ 3. Triggers post_save signal
     ▼
┌─────────────────┐
│ create_order_   │
│ notifications   │
│   (signal)      │
└────┬────────────┘
     │ 4. Creates notification with user=None
     ▼
┌─────────────────┐
│  Notification   │
│   user=None     │ ◄─── Admin notification
│   order=order   │
└────┬────────────┘
     │ 5. Context processor fetches
     ▼
┌─────────────────┐
│  Admin Navbar   │
│  Shows: "New    │
│  order from     │
│  [username]"    │
└─────────────────┘

┌─────────────────┐
│  User Navbar    │
│  Shows: Nothing │ ◄─── User doesn't see this
└─────────────────┘
```

---

## 🔄 Flow 2: Admin Confirms Order

```
┌──────────┐
│  ADMIN   │
└────┬─────┘
     │ 1. Changes status to "confirmed"
     ▼
┌─────────────────┐
│ admin_update_   │
│ order_status    │
│     (view)      │
└────┬────────────┘
     │ 2. Updates order.status
     ▼
┌─────────────────┐
│  order.save()   │
└────┬────────────┘
     │ 3. Triggers pre_save (tracks old status)
     │ 4. Triggers post_save (detects change)
     ▼
┌─────────────────┐
│ create_order_   │
│ notifications   │
│   (signal)      │
└────┬────────────┘
     │ 5. Creates notification with user=order.user
     ▼
┌─────────────────┐
│  Notification   │
│ user=order.user │ ◄─── User notification
│   order=order   │
└────┬────────────┘
     │ 6. Context processor fetches
     ▼
┌─────────────────┐
│  User Navbar    │
│  Shows: "Your   │
│  order has been │
│  confirmed!"    │
└─────────────────┘

┌─────────────────┐
│  Admin Navbar   │
│  Shows: Nothing │ ◄─── Admin doesn't see this
└─────────────────┘
```

---

## 🔄 Flow 3: User Places Reservation

```
┌──────────┐
│   USER   │
└────┬─────┘
     │ 1. Submits reservation form
     ▼
┌─────────────────┐
│ submit_         │
│ reservation     │
│     (view)      │
└────┬────────────┘
     │ 2. Creates Reservation
     ▼
┌─────────────────┐
│ Reservation.    │
│ objects.create()│
└────┬────────────┘
     │ 3. Triggers post_save signal
     ▼
┌─────────────────┐
│ create_         │
│ reservation_    │
│ notifications   │
│   (signal)      │
└────┬────────────┘
     │ 4. Creates notification with user=None
     ▼
┌─────────────────┐
│  Notification   │
│   user=None     │ ◄─── Admin notification
│ reservation=res │
└────┬────────────┘
     │ 5. Context processor fetches
     ▼
┌─────────────────┐
│  Admin Navbar   │
│  Shows: "New    │
│  reservation    │
│  from [user]"   │
└─────────────────┘

┌─────────────────┐
│  User Navbar    │
│  Shows: Nothing │ ◄─── User doesn't see this
└─────────────────┘
```

---

## 🔄 Flow 4: Admin Rejects Reservation

```
┌──────────┐
│  ADMIN   │
└────┬─────┘
     │ 1. Changes status to "rejected"
     ▼
┌─────────────────┐
│ reject_         │
│ reservation     │
│     (view)      │
└────┬────────────┘
     │ 2. Updates reservation.status
     ▼
┌─────────────────┐
│ reservation.    │
│    save()       │
└────┬────────────┘
     │ 3. Triggers pre_save (tracks old status)
     │ 4. Triggers post_save (detects change)
     ▼
┌─────────────────┐
│ create_         │
│ reservation_    │
│ notifications   │
│   (signal)      │
└────┬────────────┘
     │ 5. Creates notification with user=reservation.user
     ▼
┌─────────────────┐
│  Notification   │
│user=reservation │ ◄─── User notification
│    .user        │
│ reservation=res │
└────┬────────────┘
     │ 6. Context processor fetches
     ▼
┌─────────────────┐
│  User Navbar    │
│  Shows: "Your   │
│  reservation    │
│  was rejected"  │
└─────────────────┘

┌─────────────────┐
│  Admin Navbar   │
│  Shows: Nothing │ ◄─── Admin doesn't see this
└─────────────────┘
```

---

## 🔄 Flow 5: User Cancels Order (Special Case)

```
┌──────────┐
│   USER   │
└────┬─────┘
     │ 1. Clicks cancel button
     ▼
┌─────────────────┐
│  cancel_order   │
│     (view)      │
└────┬────────────┘
     │ 2. Updates order.status = 'cancelled'
     ▼
┌─────────────────┐
│  order.save()   │
└────┬────────────┘
     │ 3. Triggers signal
     ▼
┌─────────────────┐
│ create_order_   │
│ notifications   │
│   (signal)      │
└────┬────────────┘
     │ 4. Creates USER notification
     ▼
┌─────────────────┐
│  Notification   │
│ user=order.user │ ◄─── User notification
│   order=order   │
└─────────────────┘
     
     ┌─────────────────┐
     │  cancel_order   │
     │     (view)      │
     └────┬────────────┘
          │ 5. Also manually creates ADMIN notification
          ▼
     ┌─────────────────┐
     │  Notification   │
     │   user=None     │ ◄─── Admin notification
     │   order=order   │
     └─────────────────┘

Result: BOTH get notified (this is correct!)

┌─────────────────┐        ┌─────────────────┐
│  User Navbar    │        │  Admin Navbar   │
│  Shows: "Your   │        │  Shows: "[User] │
│  order was      │        │  cancelled      │
│  cancelled"     │        │  order #123"    │
└─────────────────┘        └─────────────────┘
```

---

## 📊 Context Processor Flow

```
┌─────────────────────────────────────────────────────────┐
│              navbar_counts (context_processor)           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Is user authenticated?                                  │
│         │                                                │
│         ├─ NO ──▶ Return empty context                  │
│         │                                                │
│         └─ YES                                           │
│              │                                           │
│              ▼                                           │
│         Is user staff?                                   │
│              │                                           │
│              ├─ YES ──▶ Fetch admin notifications       │
│              │          (user__isnull=True)             │
│              │          ├─ admin_unread_notifications_  │
│              │          │  count                         │
│              │          └─ admin_recent_notifications   │
│              │                                           │
│              └─ NO ───▶ Skip admin notifications        │
│                                                          │
│         Always fetch user notifications:                 │
│         (user=request.user)                             │
│         ├─ unread_notifications_count                   │
│         └─ recent_notifications                         │
│                                                          │
│         Return context to all templates                  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Notification Routing

```
┌─────────────────────────────────────────────────────────┐
│                  Notification Creation                   │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Check user   │
                    │   field      │
                    └──────┬───────┘
                           │
              ┌────────────┴────────────┐
              │                         │
              ▼                         ▼
       ┌─────────────┐          ┌─────────────┐
       │  user=None  │          │user=<User>  │
       └──────┬──────┘          └──────┬──────┘
              │                        │
              ▼                        ▼
    ┌──────────────────┐    ┌──────────────────┐
    │ Context          │    │ Context          │
    │ Processor        │    │ Processor        │
    │ filters by       │    │ filters by       │
    │ user__isnull=    │    │ user=request.    │
    │ True             │    │ user             │
    └────────┬─────────┘    └────────┬─────────┘
             │                       │
             ▼                       ▼
    ┌──────────────────┐    ┌──────────────────┐
    │  Admin Navbar    │    │  User Navbar     │
    │                  │    │                  │
    │  ┌────────────┐  │    │  ┌────────────┐  │
    │  │ 🔔 Badge  │  │    │  │ 🔔 Badge  │  │
    │  │    (3)    │  │    │  │    (2)    │  │
    │  └────────────┘  │    │  └────────────┘  │
    │                  │    │                  │
    │  Dropdown:       │    │  Dropdown:       │
    │  • New order     │    │  • Order         │
    │  • New           │    │    confirmed     │
    │    reservation   │    │  • Ready for     │
    │  • User          │    │    pickup        │
    │    cancelled     │    │                  │
    └──────────────────┘    └──────────────────┘
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────┐
│              Mark Notification as Read                   │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ User clicks  │
                    │ notification │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Check: Is    │
                    │ user staff?  │
                    └──────┬───────┘
                           │
              ┌────────────┴────────────┐
              │                         │
              ▼                         ▼
       ┌─────────────┐          ┌─────────────┐
       │   YES       │          │    NO       │
       │  (Admin)    │          │   (User)    │
       └──────┬──────┘          └──────┬──────┘
              │                        │
              ▼                        ▼
    ┌──────────────────┐    ┌──────────────────┐
    │ Can mark as read │    │ Can mark as read │
    │ notifications    │    │ notifications    │
    │ where:           │    │ where:           │
    │ user__isnull=    │    │ user=request.    │
    │ True             │    │ user             │
    └────────┬─────────┘    └────────┬─────────┘
             │                       │
             ▼                       ▼
    ┌──────────────────┐    ┌──────────────────┐
    │ ✅ Success       │    │ ✅ Success       │
    │ Admin can only   │    │ User can only    │
    │ mark admin       │    │ mark their own   │
    │ notifications    │    │ notifications    │
    └──────────────────┘    └──────────────────┘
```

---

## 📈 Status Change Detection

```
┌─────────────────────────────────────────────────────────┐
│              Status Change Detection                     │
└─────────────────────────────────────────────────────────┘

1. PRE_SAVE Signal:
   ┌──────────────┐
   │ order.save() │
   └──────┬───────┘
          │
          ▼
   ┌──────────────────────────────┐
   │ track_order_status_change    │
   │ (pre_save signal)            │
   └──────┬───────────────────────┘
          │
          ▼
   ┌──────────────────────────────┐
   │ Get old status from database │
   │ Store in instance._previous_ │
   │ status                       │
   └──────────────────────────────┘

2. POST_SAVE Signal:
   ┌──────────────┐
   │ order.save() │
   └──────┬───────┘
          │
          ▼
   ┌──────────────────────────────┐
   │ create_order_notifications   │
   │ (post_save signal)           │
   └──────┬───────────────────────┘
          │
          ▼
   ┌──────────────────────────────┐
   │ Compare:                     │
   │ instance._previous_status    │
   │ vs                           │
   │ instance.status              │
   └──────┬───────────────────────┘
          │
          ├─ Same ──▶ Skip (no notification)
          │
          └─ Different ──▶ Create notification
                          for user
```

---

## 🎨 Visual Summary

```
┌─────────────────────────────────────────────────────────┐
│                    KEY PRINCIPLE                         │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  user=None           →  Admin Notification              │
│  ═══════════════════════════════════════════            │
│  • New orders                                           │
│  • New reservations                                     │
│  • User cancellations                                   │
│  • User confirmations                                   │
│                                                          │
│  user=<User>         →  User Notification               │
│  ═══════════════════════════════════════════            │
│  • Order confirmed                                      │
│  • Order ready                                          │
│  • Order completed                                      │
│  • Reservation confirmed                                │
│  • Reservation rejected                                 │
│                                                          │
│  NEVER MIX THEM!                                        │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 Quick Decision Tree

```
Need to create a notification?
│
├─ Is it a new Order/Reservation?
│  └─ YES → Signal handles it (admin notification)
│
├─ Is it a status change?
│  └─ YES → Signal handles it (user notification)
│
├─ Is it a user-initiated action admin needs to know?
│  └─ YES → Create manual admin notification
│
└─ Is it a special case?
   └─ YES → Use utility functions
```

---

**Remember:** Signals handle 90% of notifications automatically!
